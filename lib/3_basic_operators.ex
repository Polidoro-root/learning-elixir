# ++ and -- to manipulate lists
[1, 2, 3] ++ [4, 5, 6]

[1, 2, 3] -- [2]

# <> to string concatenation
"foo" <> "bar"

# ! && || for comparisons

# and or not - also provides, this case expects somenthing that evaluates boolea as their first argument

true and true

false or is_atom(:example)

1 and true # error
